package com.tony.clientes.trainingAlgaWorks._validationGroups;

public interface ValidationGroups {
    /**
     * Criando um interface dentro de outra inteface pemite- nos ter um grupo
     * de validações
     */

     public interface CustomClienteID {
        
     }
}
